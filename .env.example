# Go-Postfixadmin Environment Variables
# Copy this file to .env and configure as needed.

# Database Configuration
# Format: user:password@tcp(host:port)/dbname?args
DATABASE_URL="postfix:postfixPassword@tcp(localhost:3306)/postfix?charset=utf8mb4&parseTime=True&loc=Local"

# Database Driver: mysql or postgres (default: mysql)
DB_DRIVER=mysql

# Server Port (default 8080)
PORT=8080
# Alternatively, use APP_PORT if PORT is not set
# APP_PORT=8080

# Session Secret Key
# Generate a random 64-character hex string for production use
# E.g., using `openssl rand -hex 32`
SESSION_SECRET=your_super_secret_session_key_here

# SSL Configuration (Optional)
# If both CERT and KEY are provided, SSL will be enabled automatically
# SSL_CERT=/path/to/certificate.crt
# SSL_KEY=/path/to/private.key
